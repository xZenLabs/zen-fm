-- Replace this fail-closed value with the 32-byte Ed25519 release public key
-- before publishing the first release. Private signing material must never be
-- committed to the repository.
return "UNCONFIGURED"
